#!/usr/bin/python3
"""Write a Python script that, using this REST API,
and return information about the progress of your TODO list.."""
if __name__ == "__main__":
        
        import requests
        import sys
        did = sys.argv[1]
        url = request.get("https://jsonplaceholder.typicode.com/users/{}"
                          .format(did))
        nombre
